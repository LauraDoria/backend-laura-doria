//Autenticación para rutas de acceso limitado
function authentication(username, password) {
    if (username == 'pepe' || password == 'pepePassword') return isAdmin = true
}

export { authentication }